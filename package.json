{
  "name": "glenstack",
  "private": true,
  "devDependencies": {
    "husky": "6.0.0",
    "lerna": "4.0.0",
    "lint-staged": "10.5.4"
  },
  "scripts": {
    "postinstall": "lerna bootstrap && husky install",
    "coverage": "lerna run coverage",
    "validate": "lerna run validate"
  }
}
